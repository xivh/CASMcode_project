
.. _casm-project:

CASM Project
============

The :class:`~casm.project.Project` class is the main interface to a CASM project. It makes it easier to read and write project data using the standard CASM project structure, and provides access to the most commonly used methods.

Project initialization
----------------------

Use :func:`~casm.project.Project.init`....
